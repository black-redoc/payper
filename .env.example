# Environment configuration
NODE_ENV=development

# Database configuration for Prisma CLI (used in development and CI/CD)
DATABASE_URL="file:./dev.db"

# Cloudflare D1 Database Configuration (for production deployment)
# Get these values from: npx wrangler d1 list
D1_DATABASE_NAME="your-database-name"
D1_DATABASE_ID="your-database-id"
